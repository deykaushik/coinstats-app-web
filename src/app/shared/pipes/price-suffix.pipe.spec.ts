import { PriceSuffixPipe } from './price-suffix.pipe';

describe('PriceSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
